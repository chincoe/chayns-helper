/**
 * An enum for request.fetch logLevels
 */
declare enum LogLevel {
    info = 'info',
    warning = 'warning',
    error = 'error',
    critical = 'critical',
    none = 'none',
}

export default LogLevel;
