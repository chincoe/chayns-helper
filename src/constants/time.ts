/**
 * Constant for time based on ms
 * @type {{hour: number, day: number, second: number, minute: number}}
 */
const time = {
    second: 1000,
    minute: 1000 * 60,
    hour: 1000 * 60 * 60,
    day: 1000 * 60 * 60 * 24
};

export default time;
